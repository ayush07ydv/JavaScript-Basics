let Ayush; // This is Variable to store any value

Ayush = 5; // Using this method we can assign any type of value.
alert(Ayush) // using this we can call the variable value.

let Shoa, ANi, abst ; // By this method we can create multiple variables

let hsi = 'Hello',
    ghs = 56,
    oed = true;     // As you can see we can add multiple variables also and we can assign also during initialisation


let abd;
abd = 'Hello';
alert(abd)
abd = 'World';  // Here you can see that in "let" we can re-intialize also...
alert(abd);

//We can also declare two variables and copy data from one into the other.
let hello = 'Console';
let World;

World = hello;
